{
  "name": "recuperar-historico-copilot",
  "displayName": "Recuperar Historico Copilot",
  "description": "Extensión para recuperar los chats previos con Copilot",
  "version": "0.0.1",
  "engines": {
    "vscode": "^1.109.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onStartupFinished",
    "onChatParticipant:recuperar-historico-copilot.recuperador"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "chatParticipants": [
      {
        "id": "recuperar-historico-copilot.recuperador",
        "name": "historico",
        "fullName": "Recuperador Historico",
        "description": "Lee sesiones previas (.jsonl) de Copilot y recupera contexto relevante"
      }
    ],
    "commands": [
      {
        "command": "recuperar-historico-copilot.helloWorld",
        "title": "Hello World"
      },
      {
        "command": "recuperar-historico-copilot.selectChatModel",
        "title": "Historico: Seleccionar modelo de IA del participante"
      }
    ],
    "configuration": {
      "title": "Recuperar Historico Copilot",
      "properties": {
        "recuperarHistoricoCopilot.maxTokensPerChunk": {
          "type": "number",
          "default": 128,
          "minimum": 1,
          "description": "Cantidad maxima de tokens aproximados por chunk al indexar conversaciones."
        },
        "recuperarHistoricoCopilot.chunkOverlapTokens": {
          "type": "number",
          "default": 25,
          "minimum": 0,
          "description": "Cantidad de tokens de solape entre chunks consecutivos."
        },
        "recuperarHistoricoCopilot.cohereApiKey": {
          "type": "string",
          "default": "",
          "scope": "machine-overridable",
          "description": "API key de Cohere para aplicar reranking con cross-encoder. Si se deja vacio, se intentara usar la variable de entorno COHERE_API_KEY."
        },
        "recuperarHistoricoCopilot.embeddingsEndpoint": {
          "type": "string",
          "default": "http://localhost:11434/api/embeddings",
          "scope": "machine-overridable",
          "description": "Endpoint HTTP para solicitar embeddings."
        },
        "recuperarHistoricoCopilot.embeddingModel": {
          "type": "string",
          "default": "nomic-embed-text:latest",
          "description": "Modelo de embeddings usado para indexar y buscar chunks."
        },
        "recuperarHistoricoCopilot.embeddingDimensions": {
          "type": "number",
          "default": 768,
          "minimum": 1,
          "description": "Dimensiones esperadas del vector embedding devuelto por el endpoint."
        },
        "recuperarHistoricoCopilot.cohereRerankModel": {
          "type": "string",
          "default": "rerank-v3.5",
          "description": "Modelo de Cohere usado para reranking de resultados vectoriales."
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "pretest": "npm run compile && npm run lint",
    "lint": "eslint src",
    "test": "vscode-test"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.13",
    "@types/mocha": "^10.0.10",
    "@types/node": "22.x",
    "@types/vscode": "^1.109.0",
    "@vscode/test-cli": "^0.0.12",
    "@vscode/test-electron": "^2.5.2",
    "eslint": "^9.39.3",
    "typescript": "^5.9.3",
    "typescript-eslint": "^8.56.1"
  },
  "dependencies": {
    "better-sqlite3": "^12.8.0",
    "cohere-ai": "^7.21.0"
  }
}
